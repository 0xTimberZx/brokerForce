# Supabase (see docs/Architecture.md §5 — local-storage-only auth for now;
# these vars are for the database/API layer, not for user auth yet)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Database (see docs/Database.md)
DATABASE_URL=
REDIS_URL=

# Market data APIs (see docs/Architecture.md §6 for the full list — add keys as integrations are built)
COINGECKO_API_KEY=
ALCHEMY_API_KEY=

# API server
PORT=4000
